import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export function EditorPage() {
  return (
    <PlaceholderPage
      title="Editor"
      description="Review, accept, reject, or edit AI-generated resume suggestions. (M8.4)"
    />
  );
}
